{
  "name": "body-recomposition Codespace",
  "build": {
    "dockerfile": "Dockerfile"
  },
  "remoteUser": "vscode",
  "features": {},
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python"
      ]
    }
  },
  "postCreateCommand": "python3 -m pip install --upgrade pip && pip install -r scripts/requirements.txt || true",
  "forwardPorts": [],
  "workspaceFolder": "/workspaces/body-recomposition"
}
